---
description: "Add detailed documentation to a selected Python function"
name: "Doc-Func"
argument-hint: "Select a Python function to document"
agent: "agent"
---

Document the selected Python function by adding a clear and complete docstring. Include:

- A summary of the function's purpose
- `Args` section describing each parameter and its expected type
- `Returns` section describing the return value and type
- Notes for any side effects or important behavior
- Inline comments for non-obvious logic if needed

Ensure the documentation uses Google-style docstrings and return the updated function code with the new docstring.

Selected code:

{selected_code}
